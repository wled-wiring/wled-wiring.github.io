import type { EdgeTypes } from '@xyflow/react';
import EditableWire from "./EditableWire";
//import {type EditableWire} from '../../types';

export const edgeTypes = {
  "editable-wire-type": EditableWire,
} satisfies EdgeTypes;
